interface:
  display_name: "Replication Onboard"
  short_description: "Prepare a fresh clone by bootstrapping packages, data, and backends."
  default_prompt: "Use $replication-onboard to take this repo from a fresh clone to a ready replication environment."
