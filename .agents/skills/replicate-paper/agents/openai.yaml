interface:
  display_name: "Replicate Paper"
  short_description: "Run, resume, or debug the main text and IA pipelines with the right boundaries."
  default_prompt: "Use $replicate-paper to bootstrap what is missing, then run or resume this repository's replication pipeline."
